<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@drawable/ic_company_logo"
        android:label="@string/app_name"
        android:roundIcon="@drawable/ic_company_logo"
        android:supportsRtl="true"
        android:theme="@style/Theme.Teeest"
        tools:targetApi="31">

<!--         Главная активность (LoginActivity) -->
        <activity
            android:name=".LoginActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.Teeest">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Активности для городов -->
<!--        <activity-->
<!--            android:name=".MainActivity"-->
<!--            android:exported="true"-->
<!--            android:label="@string/app_name"-->
<!--            android:theme="@style/Theme.Teeest">-->
<!--            <intent-filter>-->
<!--                <action android:name="android.intent.action.MAIN" />-->
<!--                <category android:name="android.intent.category.LAUNCHER" />-->
<!--            </intent-filter>-->
<!--        </activity>-->

        <activity
            android:name=".DubaiActivity"
            android:label="@string/dubai"
            android:theme="@style/Theme.Teeest"
            android:exported="true" />
        <activity
            android:name=".StambulActivity"
            android:label="@string/stambul"
            android:theme="@style/Theme.Teeest"
            android:exported="true" />
        <activity
            android:name=".BaikalActivity"
            android:label="@string/baikal"
            android:theme="@style/Theme.Teeest"
            android:exported="true" />
        <activity
            android:name=".MurmanskActivity"
            android:label="@string/murmansk"
            android:theme="@style/Theme.Teeest"
            android:exported="true" />

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.Teeest" />

        <activity
            android:name=".TourActivity"
            android:label="@string/dubai"
            android:theme="@style/Theme.Teeest"
            android:exported="true" />
    </application>
</manifest>
